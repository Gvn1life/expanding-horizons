import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  project: "proj_ldmxaygiepjufvchkqli",
  runtime: "node",
  logLevel: "log",
  maxDuration: 900,
  dirs: ["./src/trigger"],
});
